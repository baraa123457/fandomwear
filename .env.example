# Supabase project settings — Project Settings → API in the Supabase dashboard.
# Safe to expose to the browser (protected by Row Level Security, not secrecy).
NEXT_PUBLIC_SUPABASE_URL=https://sdtklldqyakchjtmjtbm.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_e5VFLMjsGCjFbbODm-4eHQ_Qb_XLkXl

# Service role key — SERVER-SIDE ONLY. Bypasses RLS entirely.
# Never prefix this with NEXT_PUBLIC_. Never import it into client components.
# Only used by: supabase/seed.ts, and any future trusted server-only routes
# (e.g. an admin API route that needs to bypass RLS for a specific action).
SUPABASE_SERVICE_ROLE_KEY=
